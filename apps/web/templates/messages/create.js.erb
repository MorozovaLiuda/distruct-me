$('#create-message, .options, .more-options').hide();
$('#copy-link').show();
$('.form-group textarea').val('<%= Web::Routes.url(:message, id: message.id.gsub('-', '')) %>')

$('#copy-link').click(function() {
    $('.form-group textarea').select();
    document.execCommand("copy");
});
